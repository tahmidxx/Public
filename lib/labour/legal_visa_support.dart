import 'package:flutter/material.dart';

class LegalVisaSupportPage extends StatelessWidget {
  const LegalVisaSupportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Legal and Visa Support"),
      ),
      body: const Center(
        child: Text("Legal and Visa Support details will be listed here."),
      ),
    );
  }
}